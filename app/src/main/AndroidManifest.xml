<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="vn.edu.tdc.managementequipmenttdc">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Main_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.SplashActivity" />
        <activity android:name=".activities.EditProfileActivity" />
        <activity android:name=".activities.ChangePasswordActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.ListAreaOrBuildingActivity" />
        <activity android:name=".activities.TypeEquipmentActivity" />
        <activity android:name=".activities.EquipmentsActivity" />
        <activity android:name=".activities.ReportMalfunctionActivity" />
        <activity android:name=".activities.HistoryActivity" />
        <activity android:name=".activities.MyWorkActivity" />
        <activity android:name=".activities.ListRoomsActivity" />
        <activity android:name=".activities.DetailMyWorkActivity" />
        <activity android:name=".activities.HelpUserActivity" />
        <activity android:name=".activities.ListMalfunctionOneEquipmentActivity" />
        <activity android:name=".activities.ViewReportMalfunctionActivity" />
        <activity android:name=".activities.DetailMalfunctionActivity" />
        <activity android:name=".activities.ListMalfunctionOfRoomActivity" />
    </application>

</manifest>